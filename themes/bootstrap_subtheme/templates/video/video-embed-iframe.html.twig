{#
/**
 * @file
 * Display an iframe with alterable components.
 */
#}
<iframe{{ attributes.setAttribute('title', iframe_title|default('Video player')) }}{% if url is not empty %} src="{{ url }}{% if query is not empty %}?{{ query | url_encode }}{% endif %}{% if fragment is not empty %}#{{ fragment }}{% endif %}"{% endif %}></iframe>